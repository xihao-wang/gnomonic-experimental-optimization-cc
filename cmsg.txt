feat-part1: add side-by-side fisheye/composite visualization export

Add new visualization mode that saves a single image composed of two sub-images:
- Left: original fisheye with GT and detected bounding boxes
- Right: corresponding composite image with GT and detected bounding boxes
- Create new output folder structure:
  evaluation/configs-comparison/backproj_visual_test_session_N/grid-kxj-fovX/dataset_name/all-images/side-by-side
- Enable easier visual verification of correct back-projection from composite to fisheye
- Keep existing separate export folders fisheye and composite unchanged
- Implement dataset-agnostic support
- Works with current BOMNI dataset
- Supports future datasets by specifying annotation format and dataset path
- Minor refactor for cleaner and extensible visualization pipeline

feat-part2: Improve visualization consistency and readability

- visualize_composite_detections(): Accept bbox_color and bbox_thickness
  parameters to allow consistent styling across different visualizations

- create_side_by_side_visualization(): Add white header bar with labels
  instead of overlaying text on images. Header height and font size scale
  relative to image dimensions (3% height, min 30px) for compatibility
  with different dataset resolutions

- _save_composite_visualization(): Pass prediction color and thickness
  from config to ensure visual consistency between composite and fisheye
  images (both use yellow for predictions, green for GT)

Color scheme: GT (green), Predictions (yellow) - consistent across all views

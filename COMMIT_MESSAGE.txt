feat: Add evaluation framework infrastructure (Phase 4, Steps 1-4)

Create extensible evaluation infrastructure for testing multiple projection configurations.

Changes:
- Add evaluation/projection_configs/test_configs_v1.json (3 test configurations)
- Add evaluation/dataset_registry.py (dataset structure definitions, BOMNI only)
- Add evaluation/output_manager.py (path management without dataset-specific args)
- Add evaluation/visualization.py (GT + prediction visualization, composite + fisheye)
- Add plans/phase-4-evaluation-framework.md (implementation plan)

Output structure:
- configs-comparison/{json-name}/{config-id}/{dataset}/bboxes-numeric/
- configs-comparison/{json-name}/{config-id}/{dataset}/bboxes-visuals/composite/
- configs-comparison/{json-name}/{config-id}/{dataset}/bboxes-visuals/fisheye/

Visualization design:
- GT boxes drawn first (green, thick=2)
- Predictions drawn on top (red, thin=1)
- Both visible when aligned

Next: Implement evaluation runner and configuration script.

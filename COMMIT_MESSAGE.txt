fix: Resolve memory leak in evaluation framework

- Reuse DetectionPipeline object across images instead of creating 735 instances (was creating new pipeline + YOLO model per image, now creates once per config)
- Explicitly clean up large objects (composite, metadata, pipeline) after use
- Delete mapping_matrices list after copying to combined array to free memory immediately

Other improvements / minor fixes:
- Add YOLO model validation with automatic download option if unavailable at specified directory of JSON file
feat: Add two-step dataset preparation workflow with manual review

Create extensible preparation pipeline for all datasets (BOMNI, PIROPO, etc.).

Changes:
- Add datasets/prepare_dataset_step1.py (automated preparation)
- Add datasets/prepare_dataset_step2.py (semi-automated cleanup after manual review)
- Add datasets/PREPARATION_README.md (complete workflow documentation)
- Update evaluation/config.py (add PREPARATION config nodes)
- Remove legacy standalone scripts (extract, preprocess, cleanup, config)
- Remove evaluation/visualize_bomni_annotations.py (argparse violation)

Two-step workflow:
1. Step 1 generates visualizations for manual quality review
2. User manually deletes incorrect visualization images
3. Step 2 removes corresponding annotations/frames based on deletions

Architecture will and should work identically for all datasets via unified standard JSON format.

feat: Improve detection pipeline with GPU auto-detect and organized results

Auto-detect GPU/CPU device (CUDA if available, else CPU). Organize results in timestamped directories per image. Save single output with metadata.

Changes:
- detection-pipeline/config.py: YOLO.DEVICE defaults to None for auto-detection
- detection-pipeline/yolo_detector.py: auto-detect GPU if available using torch.cuda.is_available()
- detection-pipeline/pipeline.py: refactor to save organized results in results/{image_name}/{timestamp}/ with 4 files (fisheye image, composite, detections with bboxes, metadata.txt)
- detection-pipeline/test_pipeline.py: simplify to use organized output, remove redundant saving
- detection-pipeline/README.md: update documentation for new output structure and GPU auto-detection

Test result: 3 pedestrians detected on fisheye-sample.png with GPU (CUDA), results saved to timestamped directory with metadata file
